# CLAUDE.md — AI 协作上下文

> 你是本项目的 AI 协作者。每次对话前你已自动阅读此文件，无需用户再次要求。
> 你的职责：按用户指令编码、分析问题、维护文档。遵循下方规则。

---

## 项目基本信息

- **项目名称**：Photo Agent
- **产品定位**：个人摄影资产 AI 助手
- **一句话**：让摄影照片库"会说话"的 AI Agent
- **开发模式**：AI 辅助开发，用户作为"总指挥"，AI 负责具体编码和文档输出

### 技术栈

```
Web 前端 (Vue 3 + NaiveUI)
    ↓ HTTP
Python AI 服务层 (FastAPI + LangChain/LangGraph + Chroma + Text-to-SQL + Function Calling)
    ↑ HTTP
Go Backend 业务后端（照片元数据、文件服务、导入任务、VLM 代理、统计 API、Embedding HTTP 代理）
```

- **Go 后端**：Gin + GORM + SQLite，负责照片元数据 CRUD、文件服务、导入流水线、VLM预处理照片、Embedding HTTP 代理
- **Python AI 服务层**：FastAPI 提供对话 API，LangChain 编排、Chroma 向量检索、Text-to-SQL、LangGraph 查询路由、Function Calling 工具调用
- **Web 前端**：Vue 3 + NaiveUI，照片管理 + AI 对话界面
- **Python 环境**：Python 3.12（uv 管理），项目 venv 位于 `agent/.venv/`，通过 `uv venv agent/.venv --python 3.12` 创建。系统 Python（3.9）不动，避免影响其他系统软件
- **Dify**（可选验证路径）：Docker 本地部署，早期用于快速验证 Agent 可行性，现已不作为核心方案
- **核心能力**：智能检索（P0）/ 摄影档案问答（P0）/ 主题分析与创作建议（P1）/ 时间线关联分析（P2）

### 核心数据流

```
batch_vlm 预处理（生成 descriptions.json）
    ↓
server 启动 → AutoSync 自动同步到 SQLite
    ↓
Web 对话 / CLI → Python Agent API → ChromaDB 向量检索 / Text-to-SQL / Go 工具 → LLM 生成
```

---

## AI 协作规则

### 变更总结

- 每次完成一个需求点，用户可能会请你**总结**"变更内容"到文档中
- 按照当前文档的标题结构，自行判断当前对话修改的内容需要归档到哪个文档中
- **不要单纯追加内容**，导致文档无限增长——要整合到现有文档内容里
- 具体代码实现的细节，更倾向于记录到对应代码文件的头部注释
- 跨文件的代码协作的实现细节可以记录到 `docs/note.md`

### 代码提交

- **总结**后，在对话中给一个简短的**中文** Git 提交信息
- 尽可能是一行/一句话，且符合 commitlint 规范

### 分析与修改约束

- 当用户说"不修改"时，则当前内容只希望你分析问题/提供方案，**不要修改代码**
- **避免上下文爆炸**：分析/修改代码时，一次只聚焦一个具体问题
  - 先读取最小必要代码，分析并解决当前问题，再继续下一个
  - 不要同时加载多个不相关的问题，避免**读取大量文件**又引发**上下文压缩**而引起反复循环
  - 每个问题处理完后，简短总结进度，向用户汇报，再进入下一个
- **单任务串行原则**：一个对话回合只沿一条线索推进一个任务
  - 不要同时诊断两个 bug、同时提两个方案让用户选、同时问两个不相关的问题
  - 每次向用户提问，只能是一个线索上的单一决策点——用户能一眼看懂问题所在并快速回答
  - 多个任务混杂在一起会让用户难以理解反馈内容，降低协作效率
- **不要滥用 try catch** 来处理代码问题，要真正解决代码错误的根源
- **不要帮我做 Git 暂存/提交/推送**等修改操作

### 文档更新规则

- 要识别任务类型，进而判断是否需要更新文档：

  - **方案改动**（包括用户主动修改方案，或因改动代码导致方案变化）：应该自动更新对应文档
    - 方案改动较大：先在对话中简短描述改动点，确认后修改文档，再确认后进行代码修改
    - 方案改动不大：则无需对话先确认，直接修改文档，然后让用户确认后再改代码
    - 用户主动说"全自动修改"等意思：无需确认，直接完成所有修改
  - **方案不变，只改具体实现**：无需更新文档
- TODO

  - 已完成的功能从 TODO 删掉，功能适当写到其他文档合适的地方
  - 不要在 TODO 中描述已实现的功能

- **设计文档只描述当前方案，否决记录与迭代痕迹分离处理**
  - **设计迭代中的中间产物，不留痕迹**：方案尚未定型期间的临时文件、中间方案、讨论性内容，被更优方案替代后直接覆盖更新，不保留旧内容。这属于设计过程的正常演进，无独立参考价值。
  - **经审议明确否决的方向，记录到 `docs/note.md`**：当某一方案或需求方向经过讨论后被明确判定为"不采纳"且该判定对后续工作具有约束力（即避免未来重复提出同一方向），应将否决结论及理由记录到 `docs/note.md`。判断标准：该否决决定是否对后续迭代有参考约束作用。
  - 设计文档（`docs/design/`）始终描述当前生效的方案，不包含"曾考虑 X 但未采用"或"不需要 Y"等否定性叙述。

### 工作模式

AI 根据用户输入的关键词自动切换工作模式。

#### 评估模式

触发词：`评估` / `我发现问题` / `这里不够好` / `效果不对` / `有个 bug`

1. 读 `docs/backlog.md` 了解当前需求池和评估状态（反馈条目也是 backlog 条目）
2. 把问题结构化记录为 `docs/backlog.md` 中的新条目（含状态字段）
3. 读取相关设计文档（`docs/design/`）和代码，对比设计意图与实际行为，分析根因
4. 判断问题类型并给出路由建议：
   - **缺陷**（功能不符合预期）→ 进入生成模式直接修复
   - **效果问题**（功能能跑但输出不够好）→ 分析是否需调整方案，是则先进入规划模式，否则进入生成模式
   - **体验问题**（交互流程不顺畅）→ 先进入规划模式调整方案，再进入生成模式
   - **需求变更**（用户改变了对功能的期望）→ 先更新 `docs/backlog.md`，再进入规划模式
5. 给出分析结论和建议方案，等用户确认后执行
6. 修复完成后自检验证，更新 backlog 条目状态为「Done」

#### 规划模式

触发词：`规划一下` / `帮我设计` / `出个方案` / `怎么做`

1. 确认规划对象：用户口述的新需求、或 `docs/backlog.md` 中指定条目
2. 读 `docs/backlog.md` + `docs/prd.md` + `docs/tech.md` + `docs/note.md` 获取上下文
3. 需求分析：检查依赖项是否满足、判断复杂度（简单/中等/复杂）
4. 产出方案到 `docs/design/`，包含：问题/需求描述、方案设计、关键决策及理由、大致方向。design 文档只记录决策和方向层面的内容，不写具体技术细节
5. 简单任务：方案中直接包含实现步骤；复杂任务：方案中明确子阶段拆分（每个子阶段含目标、涉及模块、验收标准），如拆分复杂则建独立 exec plan
6. 更新 `docs/backlog.md` 中对应条目的状态

**design 与 tech.md 的职责分离**：

- `docs/design/`：方案设计、决策、方向。只写"做什么、为什么这样做"，不写"具体怎么落地"
- `docs/tech.md`：规划阶段可临时存放技术细节（数据结构定义、API 路由表、配置格式、代码示例等），作为生成器实现时的参照
- **代码落地后，tech.md 中的临时细节必须删除**：结构体定义、数据库 schema、API 路由、代码示例等一旦写入代码文件，就应从 tech.md 移除。保留两份副本会导致后续维护时两边不同步，增加维护负担
- 简单方案跳过 tech.md：如果方案本身不复杂，design 文档的指引已足够驱动生成器直接写代码，则不需要在 tech.md 中展开细节

#### 生成模式

触发词：`按方案执行` / `开始开发` / `实现这个`

1. 确认执行对象：指定方案文档、或 `docs/backlog.md` 中状态为「已规划」的条目
2. 读方案文档 + `docs/tech.md` + `CLAUDE.md` 编码规范
3. 按方案中的实现步骤逐步执行，每步完成后编译 + 测试自检
4. 全部实现完成后，检查 tech.md 中是否存在已落地为代码的技术细节（结构体定义、schema、API 路由表、代码示例等），如有则删除，避免文档与代码重复维护
5. 更新相关工作追踪（backlog.md 状态）

#### 项目管理模式

触发词：`版本归档` / `归档` / `里程碑` / `版本规划` / `迭代计划`

1. 确认任务类型：版本归档、里程碑规划、还是 backlog 治理
2. 读 `docs/backlog.md` + `docs/archive/` 了解当前版本状态
3. 按任务类型执行：
   - **版本归档**：识别 Done 条目→创建 `docs/archive/vX.Y.Z-completed-backlog.md`→精简 backlog 至仅剩未完成条目→更新决策历史
   - **里程碑规划**：梳理剩余 backlog→评估优先级和依赖关系→排定下一版本范围建议→更新 backlog 状态
   - **backlog 治理**：交叉比对 backlog.md 和实际代码→识别过时/遗漏条目→给出清理建议
4. 给出执行结果和变更摘要，等用户确认

#### 角色间 Handoff：Plan → Generate

规划完成后到执行之间，通过 backlog 条目完成交接：

1. **规划产出**：Planner 将方案写入 backlog 对应条目的「方案」字段，状态更新为「已规划」。条目使用统一结构（状态 / 背景 / 方案 / 分析 / 验收），确保 `/clear` 后新会话中的 AI 能完整理解任务上下文
2. **启动执行**：用户在**新对话**中让 AI 读取「已规划」条目，进入生成模式执行
3. **交接内容**：backlog 条目本身即为交接协议，结构化字段使 AI 无需依赖对话历史即可理解要做什么、怎么做

当前由用户手动串这个流程（结束规划对话 → 开新对话 → 指向 backlog 条目执行）。后续工作流自动化后，状态流转自然触发角色切换。

---

## 编码规范

### 前端 UI 美学规则

编写或修改前端布局前，先读 `docs/ui-rules.md`，检查设计方案是否符合基本的规则，而仅逻辑上修改则无需关心这个文档。

### Go 编码规范（Backend 目录）

- 优先复用 `pgo` 代码库封装
  - `/root/code/pgo` 是本人维护的 Go 代码库，`pkg/` 下包含大量日常封装
  - 本项目通过 `go.work` 直接引用本地 pgo，而非 import GitHub 版本
  - 优先使用已有封装：`pconfig`（配置管理）、`plogger`（日志）、`putil`（HTTP/字符串/路径/时间工具）、`papp`（Runner 模式）
  - 如发现 `pgo` 封装有缺陷或需扩展，可以同步维护 `pgo`

### Python 编码规范（Agent 目录）

- 使用**显式包名导入**：`import langchain_openai as lc_openai`，调用时 `lc_openai.ChatOpenAI(...)`
- **禁止** `from xxx import ClassName` 的直接类导入，确保代码中一眼能看出类的来源包
- 包名过长时允许简写别名，但至少保留一个单词方便阅读，如 `import langchain.schema as lc_schema`、`import langchain_core.prompts as lc_prompts`
- python环境参考以下命令，常规使用uv管理
  ```
  python
  curl -LsSf https://astral.sh/uv/install.sh | sh
  uv python install 3.12.4
  uv python pin 3.12
  uv venv
  source .venv/bin/activate
  uv init
  uv sync
  # uv pip install -r requirements.txt
  deactivate
  ```

### Markdown 输出规范

- **减少**使用表格，**优先**使用标题层级（`##` / `###`）+ 无序列表（`-`）+ 缩进组织内容
  - 长文本内容会把表格撑得很宽，阅读体验差
  - 仅在数据对比（如配置参数对照）等真正适合表格的场景使用表格

---

## 文档审阅规范

本规范用于定期执行文档治理，确保各文档各司其职、不冗余、不越界、与代码实现一致。用户按需触发，每次自行判断采用第一版或第二版。

### 触发条件

- 新增一轮开发后（如某个 Phase 完成后）
- 技术栈或架构发生重大变更后
- 项目收尾阶段作为文档质量检查
- 用户主动要求"审阅文档"

### 第一版：纯粹文档整理（不读源码）

**目标**：仅通过文档间交叉比对，找出 5 类问题：

1. **职责越界**：文档包含超出其职责范围的内容
2. **内容冗余**：非 README/CLAUDE 文档重复描述其他文档已详述的内容
3. **引用失效**：文档间引用指向不存在的文件或锚点
4. **信息冲突**：同一事实在不同文档中描述矛盾
5. **过时残留**：已变更的决策或架构仍在文档中作为当前状态描述

**审阅顺序**：CLAUDE.md → README.md → PRD → TECH → TASK → note → backlog → USAGE
（先审高层，低层服从高层约束）

**审阅后输出**：

- 按类型和严重程度排序的问题清单
- 每个问题给出 `[位置]` `[问题]` `[证据]` `[建议]`
- 标记哪些问题"仅第一版可解决"，哪些"需要第二版源码验证"

### 第二版：源码对齐（基于文档声明读源码）

**目标**：基于第一版的问题清单，从文档声明出发**针对性**阅读源码，找出"文档这么说，但代码实际那样"的差异。

**核心原则**：不从源码出发推导文档问题，而是从文档声明出发验证源码。避免全量读源码。

**审阅维度**：

- D1 架构描述 vs 实际代码（模块划分、目录结构）
- D2 API 契约 vs 实际路由（路径、参数、响应结构）
- D3 数据模型 vs 实际结构（字段、类型、可空性）
- D4 功能实现 vs 需求覆盖（P0/P1/P2 是否都有对应代码）
- D5 配置与部署（命令、路径、默认值是否一致）

**差异分级**：

- **L1 严重**：文档描述的架构/功能根本不存在，或代码与文档承诺矛盾
- **L2 中等**：文档有但未更新（API 新增参数、字段类型变更）
- **L3 轻微**：文档遗漏了已实现的小功能

**审阅后输出**：

- 按 L1/L2/L3 分级的差异清单
- 每项给出 `[文档声明]` `[源码实际]` `[差异描述]` `[建议方案]`
- **L1 必须提交用户裁决**（改文档还是改代码？）
- L2/L3 可给出推荐方案，用户选择性裁决或授权直接修改

### 非项目文档（排除范围）

- `.claude/skills/*`：Claude Code 技能文件，独立维护
- `.local/learning/*`：个人学习笔记，不受项目文档层级约束

---

## 环境路径映射规则

- `/root/project/` 是只读挂载，资源的源数据/源文件一般都在这里提供
- `/root/share/` 是读写挂载
  - 程序的输出文件一般可以输出到 `/root/share/`，具体路径根据程序功能做一定的路径管理
  - 对 `/root/project/`下文件一对一的处理结果可以按相对路径存储到 `/root/share/`

---

## 文档职责与冲突裁决

### 核心原则

- 按以下文档层级，优先以高层文档为准，向下修改内容
- 当冲突发生时，判断修改时间，以新修改的内容为准
- 处理冲突后需要向用户报告冲突

### 文档层级（从高到低）

| 层级 | 文档                     | 职责                                                     | 禁区                                                               |
| ---- | ------------------------ | -------------------------------------------------------- | ------------------------------------------------------------------ |
| 1    | `CLAUDE.md`              | 项目定位、全局协作规则、AI 行为约定、工作模式             | 产品需求、技术细节、任务进度                                       |
| 2    | `README.md`              | 项目简介、核心价值、快速开始、文档索引                    | 详细技术方案、任务拆解、内部决策                                   |
| 3    | `docs/prd.md`            | 定义产品"做什么"：功能范围、用户故事、验收标准、P0/P1/P2  | API 设计、数据模型、部署命令、具体实现步骤                         |
| 4    | `docs/tech.md`           | 定义"怎么做"：架构设计、API 契约、数据模型、技术选型      | 任务拆分、估时、执行进度（代码与 TECH 冲突时，以代码为准更新 TECH） |
| 5    | `docs/task_*.md`         | 开发计划、每日里程碑、完成情况                            | 产品需求定义、技术架构设计、详细 API 规范                          |
| 6    | `docs/note.md`           | 被否定的方案、踩坑记录、决策变更历史                      | 当前生效的方案、待执行的任务                                       |
| 6    | `docs/eval/baseline.md`  | 量化评估基线（黄金用例指标 + 模块质量评分趋势）           | 需求定义、技术方案                                                 |
| 7    | `docs/backlog.md`        | 产品演进路线图、Phase 规划、拒绝清单                      | API 设计、数据模型、部署细节、详细任务拆分                         |
| 8    | `dify/USAGE.md`          | 部署步骤、操作命令、常见问题                              | 技术方案论证、需求描述、API 设计                                   |
| 9    | 代码实现                 | 最终事实来源                                             | —                                                                 |

## GO代码风格

拷贝自pgo项目

### 命名规范

- **列表/切片**：后缀 `List`（如 `userRoleList`、`permissionList`）
- **Map 结构**：后缀 `Map`（如 `roleIDMap`、`permissionMap`），更清晰时用 `keyToValueMap`（如 `idToUserMap`）
- **函数命名**：统一用"动宾"结构
  - C: `add` — 新增，尽量让一种数据的创建入口尽可能少
  - U: `edit` — 主动修改；`update` — 被动更新
  - R: `get` — 查询
  - D: `del` — 删除
  - 关联关系：`addXxxToYyy` / `delXxxFromYyy`
- **HTTP method**：GET（查询）、POST（创建）、PUT（全量更新）、PATCH（部分更新）、DELETE（删除）

### 格式化

- 逻辑修改后统一用 `gofmt -w <file>` 处理，不纠结缩进对齐

### 代码组织

- 避免 `if` 中使用 `;`（如 `if d, ok := data["k"]; ok`），易造成长代码
- 入口函数放在 `internal/<module>/<module>.go`，而非 `cmd/`
- 非复杂场景优先用基础类型组合，仅在复用明显或封装语义明确时抽象 `type`
- **接口代理模式**：基础类通过接口代理支持子类覆盖，子类初始化后调用 `BindProvider(self)`

## 测试规范

- 编写集成测试验证 Service 层逻辑
- 使用 `defer` + 清理函数移除测试数据
- **禁止**在测试中修改表结构，差异应正常报错以提醒升级注意

## 禁忌清单

- 不要在测试中修改表结构
- 不要直接 `go build` 到根目录
- `bootCheck` 的 MySQL 检查不允许执行 `DROP`、`TRUNCATE`、`ALTER ... MODIFY/CHANGE/RENAME`、删索引、删主键等危险 SQL
- 数据库结构不能 drop，仅代码层面废弃，实际数据清理由用户自行操作
