# Media Agent - AI 知识框架与学习地图

> 本文档记录项目涉及的核心 AI 与工程知识，按模块组织，标注对应项目组件和预计接触时间。
> 开发过程中遇到的新概念、踩坑经验、关键理解，都补充到对应章节。

---

## 1. AI 基础概念

### 1.1 大语言模型（LLM）

- **是什么**：
- **项目应用**：Python AI 服务中的文本生成、对话回复、标签提取
- **预计接触**：Day 1（封装 LLM 调用）、Day 3（Agent 编排）
- **相关代码**：`ai/services/llm.py`

### 1.2 视觉语言模型（VLM）

- **是什么**：
- **项目应用**：将照片转为文字描述，是整套系统的数据基础
- **预计接触**：Day 1（测试 API）、Day 2（批量生成描述）
- **相关代码**：`ai/services/vlm.py`

### 1.3 Embedding（文本嵌入）

- **是什么**：
- **项目应用**：将照片描述转为向量，支撑语义检索
- **预计接触**：Day 1（初始化 Chroma）、Day 2（存入向量）
- **相关代码**：`ai/services/embedding.py`

### 1.4 Token 与上下文窗口

- **是什么**：
- **项目应用**：控制单次 API 调用的文本长度，Agent Prompt 设计需考虑
- **预计接触**：Day 1（测试 API 时）、Day 3（System Prompt 设计）

---

## 2. RAG（检索增强生成）

### 2.1 RAG 架构概述

- **是什么**：
- **项目应用**：用户提问 -> 向量检索相关照片 -> LLM 基于检索结果生成回答
- **预计接触**：Day 3（Agent 工具设计）
- **相关代码**：`ai/tools/vector_search.py`, `ai/agent/orchestrator.py`

### 2.2 向量数据库（Chroma）

- **是什么**：
- **项目应用**：存储照片描述的向量，支持相似度检索
- **预计接触**：Day 1（初始化）、Day 2（写入）、Day 3（查询）
- **相关代码**：`ai/services/vector.py`

### 2.3 相似度检索与 Top-K

- **是什么**：
- **项目应用**：将用户查询向量化，找出最相关的 K 张照片
- **预计接触**：Day 3（向量检索工具）
- **相关代码**：`ai/tools/vector_search.py`

### 2.4 混合检索（向量 + 关键词 + 过滤）

- **是什么**：
- **项目应用**：纯向量检索可能漏召，结合时间线标签、关键词做补充
- **预计接触**：Day 3（工具组合）、Day 4（优化）

---

## 3. AI Agent

### 3.1 Agent 范式概述

- **是什么**：
- **项目应用**：Media Agent 的核心交互层，理解用户意图并调度工具
- **预计接触**：Day 3（搭建 Agent）
- **相关代码**：`ai/agent/orchestrator.py`

### 3.2 ReAct 模式（Reasoning + Acting）

- **是什么**：
- **项目应用**：Agent 先思考再行动，多步解决问题
- **预计接触**：Day 3

### 3.3 Tool Calling / Function Calling

- **是什么**：
- **项目应用**：定义工具（向量检索、时间线查询、标签查询），Agent 按需调用
- **预计接触**：Day 3
- **相关代码**：`ai/tools/*.py`

### 3.4 System Prompt 工程

- **是什么**：
- **项目应用**：定义 Agent 的角色、能力边界、工具使用规则、输出格式
- **预计接触**：Day 3
- **相关代码**：`ai/agent/prompts.py`

### 3.5 多轮对话与上下文管理

- **是什么**：
- **项目应用**：保存会话历史，支持追问和澄清
- **预计接触**：Day 3（Go 会话管理）、Day 4（CLI 交互）
- **相关代码**：`backend/internal/service/session.go`, `cli/commands/chat.py`

---

## 4. AI 服务工程化（Python）

### 4.1 FastAPI 异步框架

- **是什么**：
- **项目应用**：Python AI 服务的 HTTP 接口层
- **预计接触**：Day 1（搭骨架）
- **相关代码**：`ai/main.py`

### 4.2 OpenAI SDK 调用

- **是什么**：
- **项目应用**：统一封装 LLM / VLM / Embedding 调用
- **预计接触**：Day 1
- **相关代码**：`ai/services/llm.py`, `ai/services/vlm.py`, `ai/services/embedding.py`

### 4.3 LangChain 框架

- **是什么**：
- **项目应用**：Agent 编排、工具绑定、链式调用
- **预计接触**：Day 3
- **相关代码**：`ai/agent/orchestrator.py`

### 4.4 流式输出与 SSE

- **是什么**：
- **项目应用**：对话响应实时展示
- **预计接触**：Day 4（完善）

### 4.5 API 降级与容错

- **是什么**：
- **项目应用**：模型切换、超时重试、错误处理
- **预计接触**：Day 4

---

## 5. 后端工程（Go）

### 5.1 Gin HTTP 框架

- **是什么**：
- **项目应用**：Go 后端路由、中间件、请求处理
- **预计接触**：Day 1
- **相关代码**：`backend/internal/api/routes.go`

### 5.2 GORM 与 SQLite

- **是什么**：
- **项目应用**：照片元数据、会话记录的持久化
- **预计接触**：Day 1（模型定义）、Day 2（CRUD）
- **相关代码**：`backend/internal/model/*.go`

### 5.3 REST API 设计

- **是什么**：
- **项目应用**：Go 与 Python、CLI 之间的接口契约
- **预计接触**：Day 1（定义）、Day 2-4（完善）

### 5.4 配置管理（Viper）

- **是什么**：
- **项目应用**：API Key、服务地址等配置
- **预计接触**：Day 1
- **相关代码**：`backend/config/config.go`

---

## 6. CLI 与交互

### 6.1 Click 命令框架

- **是什么**：
- **项目应用**：CLI 的命令定义和参数解析
- **预计接触**：Day 1
- **相关代码**：`cli/main.py`, `cli/commands/*.py`

### 6.2 Rich 终端美化

- **是什么**：
- **项目应用**：表格、面板、进度条、对话样式
- **预计接触**：Day 4
- **相关代码**：`cli/commands/chat.py`

### 6.3 交互式对话设计

- **是什么**：
- **项目应用**：`chat` 命令的多轮交互体验
- **预计接触**：Day 3-4

---

## 7. 数据流水线

### 7.1 照片导入流程

- **是什么**：
- **项目应用**：扫描 -> 创建元数据 -> AI 描述 -> 向量入库 -> 更新状态
- **预计接触**：Day 2
- **相关代码**：`cli/commands/photo.py`

### 7.2 断点续传设计

- **是什么**：
- **项目应用**：导入中断后可恢复，避免重复处理
- **预计接触**：Day 2

### 7.3 批量处理与并发控制

- **是什么**：
- **项目应用**：控制 API 调用频率，避免费用过高
- **预计接触**：Day 2

---

## 8. 部署与工程化

### 8.1 Docker 容器化

- **是什么**：
- **项目应用**：Go 后端、Python AI 服务分别打包
- **预计接触**：Day 5

### 8.2 docker-compose 编排

- **是什么**：
- **项目应用**：一键启动多服务 + 数据卷
- **预计接触**：Day 5

### 8.3 API Key 安全管理

- **是什么**：
- **项目应用**：环境变量配置，不硬编码
- **预计接触**：Day 1

---

## 附录：踩坑记录

> 开发过程中遇到的具体问题、原因分析、解决方案。

### 待补充...
