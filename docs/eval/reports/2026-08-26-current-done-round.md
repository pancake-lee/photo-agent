# 当前 Done 开发轮次评估

> 评估对象：PS7-PS9、GR1、CH1、TF1-1 至 TF1-10。
> 评估报告 ID：`2026-08-26-current-done-round`

## 摘要

- **总体评分：8.1/10，✅ 通过**（阈值 6.0）。
- **GR1：8.5/10，✅ 已满足 Done 条件**。三 Collection 的数据态、函数级检索与 HTTP 契约均由真实图库种子用例通过验证。
- 本轮已把选图、已选编辑、黄金用例维护和组图检索串成可用闭环；失分主要来自关键 UI 路径尚无可重复的浏览器级回归，以及黄金用例管理页持续膨胀。

## 分维度评分

### 代码质量：7.6/10

- **正确性：8.5**。`EmbedQueue` 同步 fine/coarse 组集合时清除失效组、从全量集合复用封面向量，并在封面重嵌时双写；`photo_rag` 以粒度明确路由集合。三层回归分别验证数据态、检索首位及 HTTP 契约，全部通过。
- **健壮性：7.5**。选图层保存窗口外预选项并保留原粒度；组集合启动同步可补齐存量数据。失分点是 UI 关键路径尚未有自动化回归，取消、刷新、路由离开后的真实浏览器行为仍主要依赖人工确认。
- **可维护性：7.0**。`SelectedPhotoList`、`PhotoPickOverlay` 和 `SuggestPhotoPicker` 形成了明确的复用边界，评估脚本也按 L0/L1/L2 分层。失分点是 `GoldenQueryManagement.vue` 已 1408 行，职责仍集中。
- **简洁性：7.5**。黄金用例已统一按单张评估，移除了不必要的粒度编辑；主题发现复用覆盖层避免复制列表。失分点是页面级状态、会话恢复和评估展示叠加在少数大组件中。

### 功能效果：8.3/10

- **准确性：8.5**。`eval_regression.py --level all` 对两条真实种子用例的 L0/L1/L2 均 PASS；GR1 的三个集合不再是仅代码存在而未入库。聊天端按当前客户端地址重写缩略图 URL，避免持久化 `127.0.0.1` 地址造成远程加载失败。
- **完整性：8.0**。已覆盖三集合、封面组展示、组内展开、删除清理、重建同步、单条评估、用例新建/更新和选图迁移。失分点是覆盖层、精选和聊天附件的验收仍缺浏览器交互实证。
- **一致性：8.5**。选图统一复用图片管理浏览能力，已选区域复用 `PhotoCard` / `BurstGroupModal`，主题发现通过薄封装接入同一覆盖层；全量检索路径保持独立。

### 用户价值：7.8/10

- **惊喜度：7.5**。组图检索把连拍簇压占 Top-K 的问题转为可选粒度，对大图库有明显信息密度增益。
- **可用性：8.5**。无需手改 JSON 即可新建、编辑和单条运行黄金用例；组图结果能直接展开并精选；选图跨页面复用完整图库交互。
- **交互体验：7.5**。覆盖层保留筛选、分段、区间选择和预填充，聊天缩略图补足了视觉反馈。失分点是尚未以真实浏览器流程验证刷新恢复、路由切换与连拍精选。
- **AI 增量：7.5**。评估框架没有调用 LLM 但为 AI 检索提供稳定回归锚点；GR1 的组级集合能减少上下文冗余。

## 分任务结论

- PS7 8.0、PS8 8.0、PS9 8.0：控件抽取与迁移完成，构建通过；关键交互仍待端到端验证。
- GR1 8.5：三集合写入、清理、同步、检索与前端组展示均已实现；真实种子回归全通过，应由 WIP 改为 Done。
- CH1 8.4：单张检索附件已补缩略图，且 URL 按当前运行环境生成；未作浏览器点击预览/下载回归。
- TF1-1 至 TF1-10：7.8–8.5。数据契约、L0/L1/L2 脚本、种子用例、API 和管理界面贯通；TF1-9 已将先前的组粒度评估口径收敛为单张照片，相关实现和测试一致。

## 执行证据

- `agent/.venv/bin/python3 -m unittest tests.test_evaluation tests.test_eval_regression -v`：12/12 PASS。
- `agent/.venv/bin/python3 scripts/eval_regression.py -c ../.local/pancake.yaml --level all`：L0/L1/L2 各 2 条种子用例 PASS。
- `GOTOOLCHAIN=local go test ./...`：PASS。
- `pnpm exec vue-tsc --noEmit && pnpm build`：PASS。
- backend、agent、web 在 10004、10005、10006 返回 HTTP 200。

## 已登记问题

- P1：关键 UI 交互缺少可重复的浏览器级回归。
- P2：黄金用例管理页单文件过大。
- P2：设计/验收状态存在未回写的文档一致性问题。
