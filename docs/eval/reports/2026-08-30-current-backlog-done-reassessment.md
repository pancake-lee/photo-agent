# 当前 Backlog Done 任务复评

> 评估对象：当前 backlog 全部 22 个 `Done` 条目。`暂缓` 的 BQ3、CQ4 明确排除。
>
> 评估报告 ID：`2026-08-30-current-backlog-done-reassessment`。

## 摘要

- **总体评分：8.8/10，✅ 通过。** 所有纳入条目均高于 6.0，且没有 1 至 3 分维度，维持 `Done`。
- 本轮后端、前端与 Agent 自动验证全部通过。BQ10、BQ11 已补上迁移 schema 基线和 VLM 队列公开入口的端到端闭环，解除 BQ6 在上一轮复评中的主要失分点。

## 分维度评分

### 代码质量：8.7/10

得分点：

- Go 全量测试、`go vet` 以及 data/service 的 race 检查均通过。嵌入式 schema 基线使用户路径测试不再自行执行 DDL，VLM 本地替身覆盖了成功、失败和停止后的最终状态。
- 生产 Service 的数据访问已收敛到 `data` 层；静态检查未检出完整配置日志、旧 AI/VLM 列删除语句，或生产 Service 直接使用 ORM、`pdb`、生成查询对象。
- Agent 107 项测试、配置测试和规则测试均通过；Web 生产构建与 2 项 Vitest 用例通过。

失分点：

- `svc_photo.go`、`svc_burst_group.go` 仍是较大的多职责文件，阅读和后续改动成本偏高，但不影响本轮已完成任务的正确性。

### 功能效果：8.7/10

得分点：

- PS10 的请求、持久化和响应字段由 HTTP CRUD 用例覆盖，既有用户验收确认两种编辑模式都可恢复输入。
- BQ2、BQ4、BQ5 的核心风险分别由迁移代码清除、启动日志入口清除、带上下文和时限的本地 HTTP 用例证明已关闭。
- CFG1–CFG7 已形成单一公共模板、加载期校验、价格单位契约及端口派生关系；配置与评估规则用例保持绿色。

失分点：

- 本轮 `make status` 显示 Agent 进程未运行，因此未新增依赖真实 Agent 服务的浏览器运行时证据；这一状态不否定已通过的隔离自动测试。

### 用户价值：8.5/10

得分点：

- 草稿恢复避免创作输入丢失，VLM 队列、上传删除和 Embedding 失败路径均已有可重复回归证据，降低摄影库长期维护的不可预期性。
- 配置、数据目录和工具入口收敛后，用户调整模型、路径、端口和评估用例时更容易定位唯一来源。

失分点：

- 本轮不重启服务、不发起真实模型请求，因此不对实时生成等待体验和外部模型输出质量加分。

## 分任务结论

- **PS10 8.7**：编辑输入的保存、读取和模式恢复完整，自动和人工验收链齐全。
- **BQ1 8.7，BQ2 9.2，BQ4 9.1，BQ5 9.0，BQ6 9.0，BQ8 8.7，BQ10 9.0，BQ11 9.0**：后端治理问题均有对应静态或自动证据；BQ6 的两个子项已完成闭环。
- **BQ9 8.7**：脚本自检和稳定分组仍有效；本轮当前输出为 58 个纳入文件。
- **DOC2 8.6，TIDY1 8.7，TIDY2 8.5，TIDY3 8.7，TIDY4 8.7**：路径、数据归属和文档链路清晰；不重新搬迁运行数据，仅核对当前引用与自动回归。
- **CFG1 8.9，CFG2 9.0，CFG3 9.0，CFG4 8.7，CFG5 8.8，CFG6 8.7，CFG7 8.7**：配置重复、无消费者键、校验和单位契约问题已关闭，仍保留 pgo 兼容段这一明确边界。

## 执行证据

- `GOTOOLCHAIN=local go run ./tools/go/main.go --self-check`：通过，当前输出 58 个纳入文件。
- `backend/`：`GOTOOLCHAIN=local go test -count=1 ./...`、`go vet ./...`、data/service `-race` 均通过。
- `web/`：`pnpm build` 通过，`pnpm test` 为 2/2 通过。
- `agent/`：全量单测 107/107 通过；配置测试 2/2、评估规则测试 14/14 通过。
- `make status`：backend、web 进程存在；Agent 进程已退出，故本轮未把服务探测失败计为功能失败证据。
