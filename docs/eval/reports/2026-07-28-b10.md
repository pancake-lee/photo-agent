# B10 评估报告

- **报告 ID**：eval-B10-2026-07-28
- **日期**：2026-07-28T00:10:00
- **对象**：B10 shot_at Unix 时间戳→日期转换修复 + B8/B9/R6 累积改进

## 摘要

**总分 8.0/10 ✅ 通过**（阈值 6.0）。

B10 修复是全链路日期逻辑的关键补丁，效果显著。修复前 Stage 2 时间跨度始终为 0、B9 后处理静默跳过、LLM 看到的日期是无意义数字；修复后 Stage 2 跨度 144~420 天、B9 date_map 正常构建、LLM 收到可读日期。9 轮提案全部走三阶段主路径，48 张照片全部有效（B8 正确捕获 12 次数字索引幻觉），B9 无警告（所有提案天然满足 ≥7 天）。核心评分 8.0/10，较 B10 修复前的 6.5/10 提升 1.5 分。剩余问题：数据质量（1 张照片 shot_at=0→1970）、B9 后处理替换逻辑未触发验证、1/9 提案仍有颜色属性归类倾向。建议：考虑为 _parse_shot_date 加合理日期下限（如 2000-01-01），将 B9 后处理提取为独立函数。

## 分维度评分

### 代码质量

#### 正确性 8

得分点：

- _parse_shot_date 正确处理 Unix 时间戳（'1780733203'→2026-06-06）、ISO 日期（'2025-05-02'）、空值/非法值（返回 None）三种情况
- 全链路 8 处调用点全部替换，无遗漏的裸 shot_at[:10] 或 fromisoformat(shot_at[:10]) 调用
- Stage 2 日期跨度从 0 天修复为真实值（144~420 天，正常照片范围内）
- B9 post-processing date_map 正常构建，9/9 提案的 B9 逻辑路径正确执行（无异常跳过）

失分点：

- DSC_0390.jpg 的 shot_at 为 Unix 时间戳 0（1970-01-01），导致该照片被 RAG 匹配时 Stage 2 跨度虚高至 20519 天。这不影响最终提案质量，但暴露了数据质量问题
- B9 后处理在 9/9 提案中均未被触发（所有提案天然满足 ≥7 天），后处理替换逻辑的边界情况未得到运行时验证

#### 健壮性 7

得分点：

- _parse_shot_date 对三种格式（时间戳/ISO/非法值）均有明确的失败处理，不会抛异常
- B9 date_map 为空时新增 warning 日志（之前是静默 pass，问题完全不可见）
- B8 多层降级：无效 ID 替换 → 不足 5 张补充 → 完全为空时回退到扩展列表前 5 张

失分点：

- _parse_shot_date 对 Unix 时间戳 0（1970-01-01）不做合理性校验，可能接受无意义日期。可考虑加下限阈值（如 2000-01-01）
- B9 后处理在扩展候选池时间跨度 < 7 天时记录 warning，但该分支在 9 轮测试中未触发，实际行为未验证
- find_temporal_patterns 的日期解析修复未做运行时验证（因为当前走三阶段主路径，不回退到 legacy 路径）

#### 可维护性 8

得分点：

- _parse_shot_date 作为单一工具函数，职责清晰，文档注释说明了两种格式的兼容原因
- B8/B9 逻辑分块清晰，注释标记明确
- itertools.chain 替代 list()+list() 全量拷贝，更符合 Python 惯用写法

失分点：

- B9 后处理块约 40 行，逻辑较复杂（时间跨度计算 + 候选池排序 + 两头替换），可提取为独立函数 _enforce_time_span

#### 简洁性 8

得分点：

- _parse_shot_date 实现简洁（15 行），无冗余分支
- itertools.chain 避免了不必要的列表拷贝
- 所有日期处理点从 3-5 行重复代码缩减为 1-2 行函数调用

失分点：

- import itertools 仅用于一处（itertools.chain），略显重


### 功能效果

#### 准确性 8

得分点：

- 9 条提案均为编辑视角，各有 5-6 张照片，叙事角度具体可操作
- Stage 3 prompt 中日期从无意义数字（'1780733203'）变为可读日期（'2026-06-06'），LLM 可利用日期信息做多样性判断
- 17/17 photo_id 经数据库验证全部有效（R2+R3）

失分点：

- 9 条提案中 3 条触发了 B8 幻觉捕获（33%），LLM 仍会返回数字索引而非 UUID——但所有幻觉均被正确替换，最终输出无影响
- 20519 天跨度的 Stage 2 扩展池（由时间戳 0 的照片引起）虽不影响最终提案，但可能误导日志阅读者

#### 完整性 8

得分点：

- 三阶段主路径端到端跑通，3/3 轮次均产出 3 条编辑视角提案
- 原始四项验收标准全部满足：≥5 张照片 ✅，时间跨度 ≥7 天 ✅，叙事角度有发布价值 ✅，photo_id 经数据库校验 ✅
- B10 修复解除了 Stage 1 日期分组、Stage 2 日期跨度、Stage 3 prompt 日期、B9 后处理四条路径的阻塞

失分点：

- find_temporal_patterns（legacy 回退路径）的日期修复未做端到端验证

#### 一致性 9

得分点：

- 输出 JSON 结构与之前版本完全兼容
- pipeline 字段正确标记为 'editorial_three_stage'
- 提案格式（title/angle/rationale/photo_ids/category）保持不变
- 9 条提案中无 pipeline 回退（100% 走三阶段主路径）


### 用户价值

#### 惊喜度 8

得分点：

- '新旧交织的城市面孔'：老城巷弄与新兴高层并置，讲述城市更新中割裂又共生的两种节奏，有编辑巧思
- '注视的瞬间'：串联人与动物直视镜头的照片，探讨不同对象在镜头前的注视姿态，跨场景连接感强
- '蛋糕仪式时光'：从不同年份生日蛋糕切入，展现家庭仪式在时间中的延续，情感深度好
- '山水的静默守望'：自然山水与人文元素的对比，有禅意
- 9 条提案均有跨场景/跨时间的非显性关联，优于旧版单一属性归类的范式

失分点：

- '红白映像'本质上仍是按颜色属性归类（与旧版共享属性范式类似），角度描述偏泛化
- 1/9 提案（~11%）仍有共享属性归类的倾向，未完全达到'发现用户没想到的角度'的初心

#### 可用性 8

得分点：

- 标题精炼有记忆点（'新旧交织的城市面孔'、'注视的瞬间'、'山水的静默守望'）
- 叙事角度具体可操作，不只是泛化归类
- 发布理由有说服力
- 5-6 张照片的体量适合直接发布为一条图文

失分点：

- 提案之间偶尔有主题重叠（如 R1 和 R2 都有'注视'相关主题），跨轮次调用时缺乏去重机制

#### AI 增量 8

得分点：

- Stage 1 随机采样引入意外组合，LLM 从中发现跨上下文线索（如从老城+高层→'城市的新与旧'，从蛋糕→'仪式感'），这是纯统计方法做不到的
- Stage 3 按叙事逻辑排列照片序列，体现编辑思维
- LLM 现在能利用可读日期信息做时间多样性判断，AI 增量进一步提升

失分点：

- 当 Stage 2 扩展列表时间跨度不足时，LLM 没有能力突破候选池限制——此时 AI 增量有限，依赖 B9 后处理兜底

## 下一步建议

- 数据质量：DSC_0390.jpg 的 shot_at 为 Unix 时间戳 0（1970-01-01），EXIF 日期缺失。该照片被 RAG 匹配时导致 Stage 2 日期跨度虚高（20519 天）。不影响最终提案质量，但建议在 Go 后端导入时对无效 EXIF 日期做空值处理而非默认 0
- B9 后处理替换逻辑（时间跨度不足时从扩展池找最早/最晚照片替换）在 9 轮测试中均未触发，其边界情况的正确性未得到运行时验证。可考虑写单元测试覆盖
- find_temporal_patterns（legacy 回退路径）的日期解析同样依赖 _parse_shot_date，但三阶段主路径从未回退，该路径的日期修复未做端到端验证
- 跨轮次提案去重：R1 的'注视的瞬间'和 R2 的'温馨的注视'有主题重叠，多次调用时可能产出相似选题。若未来需要多次调用攒选题池，可考虑加去重
