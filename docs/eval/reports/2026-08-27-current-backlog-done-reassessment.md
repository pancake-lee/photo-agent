# 当前 Backlog Done 任务复评

> 评估对象：评估开始时 backlog 中的 10 个 Done 任务，AQL1-1 至 AQL1-8、CH2、HW1。
>
> 评估报告 ID：`2026-08-27-current-backlog-done-reassessment`。

## 摘要

- **总体评分：8.4/10，✅ 通过**。10 项既有 Done 任务均高于 6.0，且没有 1 至 3 分维度，维持 Done。
- AQL 闭环的核心证据完整：两份黄金快照均记录 `DSC_1813` 健康且可信，P@10/R@10 为 0.714，MRR 为 1.0；留存的 fine/coarse Trace 均召回该照片。
- 本轮新增 QA2 与 DOC1 两项 P2 待规划问题。因此，“原有 10 项均 Done”成立，但当前 backlog 不再是“所有任务都 Done”。

## 分维度评分

### 代码质量：8.4/10

- **正确性：8.5**。Go 全量测试和 vet 通过，AQL 定向 Python 测试 20/20 通过；实时描述质量和 Chroma 描述版本在检索、批量审查与 Trace 中有共同证据。
- **健壮性：8.4**。JPG 输入预检、异常描述隔离和版本不一致过滤均被实现和定向测试覆盖；未启动服务，不产生新的模型调用或后台进程。
- **可维护性：8.3**。AQL1-2 的持久化状态设计已被实时状态方案收敛，处理履历与当前准入语义分离清晰；原任务的历史描述仍降低可追溯性。
- **简洁性：8.2**。批量审查、详情修复和公共图片列表复用已有入口，避免新增问题工作区；仍保留了部分已被后续方案取代的状态表述。

### 功能效果：8.5/10

- **准确性：8.6**。黄金快照中 `DSC_1813` 的实时质量、描述版本和向量版本均可追溯，MRR=1.0；fine/coarse 对话均返回该目标照片。
- **完整性：8.5**。从 VLM 输入预检、描述质量闸门、Embedding 准入，到黄金评估、Trace 和聊天粒度呈现均形成闭环。当前全量 Python 门禁不绿，削弱整体交付完整性。
- **一致性：8.4**。`PhotoThumbList` 已被聊天、聚类和黄金用例等入口复用；AQL 三份设计文档的验收状态仍与 backlog 冲突。

### 用户价值：8.3/10

- **惊喜度：7.9**。异常资产从黑盒处理结果变成可追溯、可原地修复的对象，对摄影库的长期可用性有明显价值。
- **可用性：8.5**。用户可在原有列表、详情和批量按钮完成发现、确认和修复，且聊天回复保留实际检索粒度。
- **交互体验：8.4**。已有用户 Web/浏览器验收，聊天滚动边界、图片比例与原图下载行为均已覆盖；本轮不重新启动 Web 服务做视觉复验。
- **AI 增量：8.2**。AI 描述和向量真正参与检索，但本地质量闸门和版本快照使其结论能被审计，而非只依赖模型返回文本。

## 分任务结论

- **AQL1-1：8.6**。请求材料只接受可解码 JPG，并采用请求级临时文件，覆盖损坏和同名隔离。
- **AQL1-2：7.4**。无外键履历底座可用；当前健康状态后来改为实时推导，原任务的持久化状态部分已被有意收敛。
- **AQL1-3：8.5；AQL1-4：8.8**。描述质量闸门、向量版本和检索准入语义互相对应，健康资产才可检索。
- **AQL1-5：8.3；AQL1-6：8.7**。单张原地修复与批量本地审查复用既有入口，AQL1-6 具有用户 Web 验收和自动验证。
- **AQL1-7：8.5；AQL1-8：8.7**。黄金快照与对话 Trace 可解释资产质量、检索和回答；目标照片在 fine/coarse 语义下均被召回。
- **CH2：8.4**。生产构建与会话存储定向测试通过，回复粒度持久化和公共照片列表复用已落地。
- **HW1：8.4**。状态机、用户验收模板和 AGENTS/CLAUDE 一致性可验证；本轮核对双文件归一化后无差异。

## 执行证据

- `GOTOOLCHAIN=local go test ./...`、`GOTOOLCHAIN=local go vet ./...`：通过。
- `agent/.venv/bin/python -m unittest tests.test_session_store tests.test_embed_queue_health tests.test_evaluation tests.test_eval_regression`：20/20 通过。
- `agent/.venv/bin/python -m unittest discover -s agent/tests`：83/86 通过，3 项失败，见 QA2。
- `cd web && pnpm build`：通过。
- `golden/2026-08-27-6407e7ab8ef5.json` 与 `2026-08-27-f4a17f839edf.json`：目标照片 `DSC_1813` 健康、可信，P@10/R@10=0.714，MRR=1.0。

## 已登记问题

- **QA2，P2**：Text-to-SQL 全量测试中的 schema 格式化输入类型与 few-shot 数量断言落后于当前实现，使 Agent 全量测试不能成为绿色回归门禁。
- **DOC1，P2**：AQL 三份设计文档仍标为待运行时或 Web 验收，与 backlog、黄金快照、Trace 和用户验收记录冲突。
