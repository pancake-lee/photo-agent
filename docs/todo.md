# TODO

> 按优先级分层排列，同层内按建议实施顺序排列。
> 详细方案论证见 [docs/task_3.md](task_3.md) / [docs/upgrade.md](upgrade.md) / [docs/expansion_directions.md](expansion_directions.md)。

---

## 全部优化点排序

> 方法论依据：决策矩阵（感知度 30% + 成本 25% + 风险 20% + 复利 15% + 维护 10%）
> 已完成项标记 ✔，评分仅反映实施前的预期，实际效果以评估数据为准。

| 完成 | 排名 | 类别     | 优化点                              | 排序理由                                             |
| ---- | ---- | -------- | ----------------------------------- | ---------------------------------------------------- |
|      | 1    | 交互体验 | 真正的流式输出                      | 感知度极高，0.5 天低投入，打字机效果消除等待焦虑     |
| ✔   | 2    | 检索精度 | Chroma 元数据过滤利用               | 低投入高复利，为混合检索和结构化过滤铺路             |
| ✔   | 3    | 功能扩展 | LangGraph 路由融合 Function Calling | 补齐无法调用 API 的最大短板，打通 Tool 操作链路      |
|      | 4    | 稳定性   | API 鉴权                            | 局域网部署安全刚需，实现简单风险低                   |
|      | 5    | 功能扩展 | GPS 反向地理编码                    | 地名查询是高频刚需，感知度极高，EXIF 数据已有就绪    |
|      | 6    | 交互体验 | 多轮对话上下文感知                  | 自然连续对话是聊天体验基线，感知度极高               |
|      | 7    | 稳定性   | 数据备份与恢复                      | 多年照片元数据无备份风险大，实现简单仅 1 天          |
| ✔   | 8    | 检索精度 | VLM 描述结构化提取                  | 多维精确查询直接提升检索体验，打通 metadata 过滤链路 |
|      | 9    | 稳定性   | 自动降级策略                        | 单点故障不断对话，低投入换取整体鲁棒性               |
|      | 10   | 检索精度 | RAG 重排序（Rerank）                | Top-K 质量直接影响回答准确率，轻量模型本地部署可行   |
|      | 11   | 功能扩展 | 查询结果导出                        | 分享和存档是用户自然需求，生成相册/PDF 复用已有数据  |
|      | 12   | 交互体验 | 语音输入查询                        | 户外/手持场景刚需，Whisper 成熟降低实现风险          |
|      | 13   | 检索精度 | 混合检索                            | BM25 + 向量互补，复利极高但实现复杂度偏高            |
|      | 14   | 功能扩展 | 相似照片推荐                        | 相似照片发现是强感知功能，依赖 Embedding 已有基建    |
| ✔   | 15   | 技术架构 | 照片去重检测                        | MD5 文件级去重已落地，Embedding 内容级待追加         |
|      | 16   | 技术架构 | 日志轮转与分级                      | 长期运行基础保障，0.5 天极低投入                     |
|      | 17   | 功能扩展 | 多语言支持                          | 非中文用户刚需，但当前用户群以中文为主               |
|      | 18   | 技术架构 | 配置热加载                          | 开发调试友好，对终端用户感知弱                       |
|      | 19   | 稳定性   | 数据一致性保障（延后）              | Chroma/Dify/SQLite 三方同步，死数据会随时间累积      |
|      | 20   | 功能扩展 | 智能相册自动生成                    | 感知度高但实现复杂，需先稳定 RAG 和标签体系          |
|      | 21   | 技术架构 | 模型 A/B 测试框架                   | 选模型决策支撑，当前手动对比可覆盖                   |
|      | 22   | 功能扩展 | 摄影参数智能推荐                    | 新手友好但受众有限，依赖历史数据积累                 |
|      | 23   | 技术架构 | 本地 Embedding 模型                 | 降本增效但初期部署复杂，模型选型和性能待验证         |
|      | 24   | 功能扩展 | 用户偏好学习（长期记忆）            | 个性化体验天花板高，但投入大且需长期积累             |
|      | 25   | 检索精度 | 负样本学习优化 Embedding            | 误匹配是检索痛点，但需先完成负样本标注才可实施       |

> 评分机制：每维度 1-5 分，感知度/复利正向计分（越高越好），成本/风险/维护反向计分（越低越好，即分高 = 投入小、风险低、维护轻）。

---

## 人工标记延后

- [ ] 数据一致性保障（Chroma / Dify / SQLite）
  - description 更新后 Chroma 不会自动同步（需手动重跑 `index_photos.py`）
  - 照片删除后 Chroma 和 Dify 中残留死数据
  - **期望**：照片删除或描述修改后，用户不会再搜到已删除的照片或过时的描述，避免"搜到了但文件不存在"的困惑
  - 建立 `sync_state` 表或定期校验脚本，工作量约 2-3 天

---

## 高优先级

- [X] Chroma 元数据过滤利用

  - `photo_rag.py` 的 `_retrieve()` 增加 `where` 参数透传
  - `index_photos.py` metadata 增加 `shot_at` 字段，支持按时间线过滤
  - `answer_question()` 同步支持 `where` 参数，上层可直接调用
  - **期望**：用户可用时间维度精确缩小检索范围，避免"搜海边照片却返回三年前"的干扰。示例问题："2024 年 8 月在海边拍的照片"
  - 为混合检索铺路，工作量约 0.5 天
- [X] LangGraph 路由融合 Function Calling

  - 路由从 sql/rag 二分类扩展为三分类（sql / rag / tool）
  - `tool` 分支使用 `llm.bind_tools()` + OpenAPIClient 执行 Go API
  - `_tool_node` 支持多轮 tool_call → 结果回传 → LLM 总结
  - **期望**：Agent 可执行具体操作（列表、详情、归档），不再局限于问答。示例问题："列出所有时间线""把 DSC_0001.JPG 归档"
  - 补齐"列出时间线""按标签查照片""照片详情"等查询的短板，工作量约 1-2 天
- [X] 照片去重检测

  - `batch_vlm` 处理前计算文件 MD5，跳过已处理的相同文件，节省 VLM 费用
  - `AutoSync` 导入前检查 MD5，避免相同内容以不同路径重复入库
  - MD5 注册表持久化到 `dedup_hashes.json`，跨进程共享
  - `--no-dedup` flag 支持禁用去重
  - **期望**：自动跳过重复文件导入，节省 VLM 调用费用和存储空间，避免同一照片在库中出现多份副本
  - 文件级 MD5 去重已完成；内容级 Embedding 余弦相似度待后续，工作量约 1-2 天
- [X] VLM 描述结构化提取

  - description 是纯文本，Tags 字段空置，无法按维度精确检索
  - 新建 `extract_attributes.py`：LLM 提取 objects/colors/scene/lighting/mood/composition
  - 属性存入 `attributes.json`，`index_photos.py` 同步写入 Chroma metadata（配合 where 过滤）
  - Go 新增 `PUT /api/v1/photos/:id/tags` API，支持将结构化标签回写 SQLite
  - **期望**：用户可用多维度精确查询，如"找蓝色调、室外、有人物的照片"，检索精度从文本模糊匹配提升到结构化匹配
  - 工作量约 2-3 天
- [ ] RAG 重排序（Rerank）

  - 向量检索后直接取 Top-K，没有二次精排
  - 引入轻量级 Cross-Encoder（如 `BAAI/bge-reranker-base`）本地部署
  - 流程：向量召回 Top-20 → Rerank 精排 → Top-5 送入 LLM
  - **期望**：召回结果更贴合用户真实意图，减少无关照片混入上下文，LLM 最终回答更准确、更聚焦
  - 工作量约 1-2 天

---

## 中优先级

- [ ] 真正的流式输出

  - `photo_rag.py` 设置了 `streaming=True` 但 `answer_question()` 直接 `invoke()`
  - CLI 聊天循环也没有流式打印
  - 接入 `chain.astream()` + PID 打印机实现打字机效果
  - **期望**：用户提问后不必等待整段回答生成，像打字机一样逐字看到回复，交互体验更自然、感知延迟更低
  - 工作量约 0.5-1 天
- [ ] 自动降级策略

  - SQL 查询失败 → 自动降级到 RAG
  - RAG 检索结果为空 → 自动降级到 SQL 或直接回答"未找到"
  - 主模型调用失败 → 自动降级到 fallback 模型
  - **期望**：单点故障不会中断对话，用户始终能得到回答（可能精度降低），避免"服务不可用"的挫败感
  - 工作量约 0.5-1 天
- [ ] 配置热加载

  - 修改配置后必须重启 server 才能生效
  - 使用 `viper.WatchConfig()` 监听配置文件变化
  - VLM prompt / 并发数 / 日志级别等支持动态生效
  - **期望**：调整参数（如切换模型、修改 prompt）后立即生效，无需中断服务重启，开发调试和线上调参更高效
  - 工作量约 0.5-1 天
- [ ] API 鉴权

  - 所有 API 和图片文件完全开放，局域网部署有安全隐患
  - 增加 Bearer Token 中间件，配置文件增加 `server.api_key`
  - Dify 工具配置同步配置 API Key
  - **期望**：局域网或公网部署时照片和 API 不被未授权访问，保障个人隐私和照片资产安全
  - 工作量约 1 天
- [ ] GPS 反向地理编码

  - EXIF 中已有经纬度，但未转换为人类可读地名
  - 调用反向地理编码 API 转为省/市/区/景点名，存入 `location` 字段
  - 支持"我在北京拍的照片""故宫的照片"等查询
  - **期望**：用户可用自然语言地名查询照片，无需记住具体拍摄日期，地理位置成为直观的检索维度
  - 工作量约 1-2 天
- [ ] 数据备份与恢复

  - SQLite / Chroma / descriptions.json 无备份机制
  - 提供 `backup` / `restore` 子命令，打包全部数据
  - 支持定时自动备份（保留最近 N 份）
  - **期望**：误删或数据损坏后可快速恢复多年积累的元数据和向量索引，避免照片"找不到了"的永久损失
  - 工作量约 1 天
- [ ] 查询结果导出

  - Agent 返回的结果无法保存
  - 支持导出为 Markdown 相册 / PDF / 幻灯片
  - CLI 新增 `--export {format}` 参数
  - **期望**：用户可将 Agent 找到的系列照片导出为可分享的相册文档，方便整理旅行回忆、制作摄影作品集
  - 工作量约 1-2 天
- [ ] 模型 A/B 测试框架

  - 可切换模型但无系统化效果对比
  - 扩展 `evaluation.py`，多模型运行同一评估集
  - 输出准确率 / 延迟 / 费用对比报告
  - **期望**：切换模型时有量化数据支撑决策，确保新模型在准确率、延迟和成本上都更优，避免"凭感觉换模型"
  - 工作量约 1-2 天
- [ ] 负样本学习优化 Embedding

  - 评估集只有正样本，无法针对性优化
  - 增加 `irrelevant_photos` 标注，使用对比学习微调 Embedding
  - **期望**：减少"搜猫却返回狗"的误匹配，语义检索结果更准确，用户找照片的成功率提升
  - 工作量约 2-3 天（需先完成负样本标注）

---

## 低优先级

- [ ] 日志轮转与分级

  - 接入 pgo 日志体系，支持按大小/时间轮转
  - 增加日志级别配置
  - **期望**：长期运行不占用过多磁盘空间，排查问题时可通过日志级别快速定位根因
  - 工作量约 0.5 天
- [ ] 多语言支持

  - 配置 `vlm.language` 控制描述语言
  - 多语言 Embedding 模型
  - **期望**：非中文用户也能用母语（英文/日文等）自然语言查询照片库
  - 工作量约 1-2 天
- [ ] 语音输入查询

  - CLI 增加 `--voice` 模式，Whisper 语音转文本
  - **期望**：不方便打字时（如户外、手持相机）可用语音直接提问，解放双手
  - 工作量约 1 天
- [ ] 摄影参数智能推荐

  - 基于历史 EXIF 数据给出拍摄参数建议
  - 如"拍星空多用 ISO 3200 + 20s + f/2.8"
  - **期望**：新手用户可获得基于自己拍摄历史的专业参数建议，从"凭感觉拍"升级为"数据驱动拍"
  - 工作量约 2-3 天
- [ ] 用户偏好学习（长期记忆）

  - 记录查询和点击行为，构建偏好画像（题材 / 色调 / 焦段）
  - 回答时融入偏好推荐
  - **期望**：Agent 越用越懂用户，主动推荐符合个人风格的拍摄建议和相似照片，从"工具"进化为"懂你的助手"
  - 工作量约 3-5 天

---

## 已有详细规划（见 task_3.md / upgrade.md）

以下方向已有完整方案，此处不展开：

- 混合检索（task_3 Phase 1.1）
- 多轮对话上下文感知（task_3 Phase 1.2）
- RAG 评估集扩充（task_3 Phase 1.3）
- 相似照片推荐（task_3 Phase 2.1）
- 智能相册自动生成（task_3 Phase 2.2）
- 摄影报告生成（task_3 Phase 2.3）
- 时间线关联分析（task_3 Phase 2.4）
- SQLite WAL 模式（task_3 Phase 3.1 / upgrade 1）
- 异步后台同步（task_3 Phase 3.2 / upgrade 4.1）
- 内存缓存层（task_3 Phase 3.3 / upgrade 4.2）
- 可观测性接入（task_3 Phase 3.4 / upgrade 2）
- 优雅关闭与限流（task_3 Phase 3.5 / upgrade 4.4）
- 本地 Embedding 模型（task_3 Phase 4.1 / upgrade 4.5）
- 图片格式自适应（task_3 Phase 4.2 / upgrade 4.2）
- proto-first 迁移评估（upgrade 3）

---

## 遗留事项

- [ ] pgo swagger：使用 pgo 的 proto 定义导出 swagger，避免 AI 输出不稳定导致文档不同步
- [ ] 匹配时间线的逻辑优化：支持更多日期格式（"2024.01.01"、"1月1日" 等），模糊匹配连续几天到同一活动
- [ ] 本地部署 embedding 模型

---

## 代码 TODO

- [ ] `agent/embedding/embedder.py:63`：Embedding URL 写死为 `{base_url}/v1/embeddings`，应支持配置灵活指定
- [ ] `backend/internal/api/routes.go:41-45`：import API 和 batch_vlm 逻辑重复，需合并统一

---

## 黄金测试集

评估脚本 `evaluation.py` 已就绪，但 `DEFAULT_EVAL_QUERIES` 中的 `relevant_photos` 字段是空的——需要你人工标注。

**操作步骤：**

1. 先确保 Go 后端和 Chroma 有数据，启动后进入 agent 目录
2. 运行一次空评估看看当前检索结果：

   ```bash
   python chain/photo_agent.py -c ../.local/my-config.yaml --eval
   ```
3. 对于每条查询，在返回的 `retrieved_ids` 中挑出你认为是"正确结果"的 photo_id，填入 `relevant_photos`
4. 可以扩充到 20~50 条查询，覆盖不同语义场景（物体/颜色/场景/情感/时间）
5. 填充后再次运行 `--eval`，得到基线指标（Precision@K / Recall@K / MRR）
6. 切换分块策略（修改配置中 `embedding.chunk_strategy` / `chunk_size` / `chunk_overlap`），重新索引后对比评估结果

**模板格式：**

```python
{
    "question": "有猫咪的照片吗？",
    "relevant_photos": ["photo_001", "photo_042", "photo_108"],  # ← 你手动标注
},
```
