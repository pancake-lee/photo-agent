# Backlog

> 全部技术需求池，按序号排列。
> 状态流转：`待规划` → `规划中` → `已规划` → `WIP` → `Done`
> 任务详情按统一结构组织，作为角色间文件交接协议。

## 任务总览

| 状态 | 阶段 | 序号 | 任务 | 评估 |
| ---- | ---- | ---- | ---- | ---- |
| 待规划 | 导入工作流 | W12 | ImportWorkflow.vue 拆分 | |
| Done | 选图控件统一 | PS7 | 控件2 已选照片列表抽取 | |
| Done | 选图控件统一 | PS8 | 黄金用例已选区接入控件2 | |
| Done | 选图控件统一 | PS9 | 主题发现选图迁移到覆盖层 | |
| WIP | 组图检索 | GR1 | 三 Collection 向量库改造 | |
| Done | 优化评估基建 | TF1-1 | 黄金用例粒度模型 | |
| Done | 优化评估基建 | TF1-2 | 按粒度评估与单条运行 | |
| Done | 优化评估基建 | TF1-3 | 三层 CLI 测试框架 | |
| Done | 优化评估基建 | TF1-4 | 检索与连拍粒度种子用例 | |
| Done | 优化评估基建 | TF1-5 | 黄金用例评估与追加 API | |
| Done | 优化评估基建 | TF1-6 | 黄金用例管理界面 | |
| Done | 优化评估基建 | TF1-7 | 黄金用例选图复用图片管理（覆盖层） | |
| Done | 优化评估基建 | TF1-8 | 黄金用例选图连拍组粒度保持 | |
| Done | 优化评估基建 | TF1-9 | 黄金用例统一按单张照片处理 | |

> v1.0.11 已归档：CL1、CL2、PS1–PS6，详见 [v1.0.11](archive/v1.0.11.md)。
> 其余 6 项待规划任务经审阅后迁至 [docs/design/2026-08-22-future-requirements.md](design/2026-08-22-future-requirements.md)。

## 产品定位决策

**从**："个人摄影资产 AI 助手"（泛化，容易堆砌技术）
**到**：「AI 选题助手」，AI 像员工提案，用户像主编审阅。

核心 workflow：拍摄→入库→AI 定期推送选题建议（推荐照片组合 + 发角度）→用户判断选哪个、如何微调→用户自己发布。

**主动不做的事**：

- 不自动发布到社交平台（发送由用户操作）
- 不替代审美判断（AI 推荐，用户决策）
- 不做多模态检索/以图搜图（选题场景不需要）

## 拒绝清单

- 混合检索、RAG 重排序、本地 Embedding、异步后台同步、Prometheus 监控
- proto-first 迁移、语音输入、多语言支持、负样本学习优化

## 任务详情

### 选图控件统一

> 专题中枢：[docs/design/2026-08-26-3-photo-selection-controls-hub.md](design/2026-08-26-3-photo-selection-controls-hub.md)。
> 控件1（选图覆盖层）已随 TF1-7/TF1-8 落地，本阶段收敛控件2 并迁移旧选图实现。

### PS7 控件2 已选照片列表抽取

- **状态**：Done
- **方案**：[docs/design/2026-08-26-2-selected-photo-list-control.md](design/2026-08-26-2-selected-photo-list-control.md)
- **背景**：图文工坊的已选照片展示（卡片网格 + 连拍组精选 + 排序）是页面内联实现，其他功能无法复用；黄金用例新建页另写了一套行内缩略图列表，样式与能力不一致。
- **方案**：把图文工坊内联的已选照片列表抽成独立通用组件：条目分单张/连拍组两类，复用现有照片卡片与连拍组弹窗（curate 模式）实现展示、精选、移除、可选拖拽排序；组成员首次打开时懒加载。控件本身不绑定任何业务语义。
- **分析**：母体选图文工坊而非新设计，因为它是唯一已验证的完整实现，抽取比重写回归风险低。此条只做抽取与黄金用例所需的通用能力，图文工坊自身是否回迁到新组件在 PS8 完成后单独评估。
- **验收**：
  - [x] 组件可独立使用：传入单张/连拍组条目集合，展示卡片网格，连拍组显示 ×N 可点开精选
  - [x] 精选后组条目替换为选中的单张条目；任一条目可移除；可选启用拖拽排序
  - [x] vue-tsc 与 vite build 通过

### PS8 黄金用例已选区接入控件2

- **状态**：Done
- **方案**：[docs/design/2026-08-26-2-selected-photo-list-control.md](design/2026-08-26-2-selected-photo-list-control.md)
- **背景**：黄金用例新建弹窗的「已选照片与粒度」是行内列表（缩略图 + 粒度下拉 + 删除），样式与图文工坊不统一；粒度逐张手选对用户是负担。用户决策：粒度完全自动推导，UI 不再暴露粒度编辑。
- **方案**：新建弹窗已选区替换为控件2。选图覆盖层回传的单张照片映射为单张条目，连拍组粒度照片映射为组条目（组信息优先取选图回传携带的连拍组字段，缺失时按封面反查）；保存时组条目持久化为「封面 + 粒度档位」，精选后退化为单张粒度。数据层粒度字段保留，评估与检索后端口径不变。
- **分析**：粒度自动推导的语义已由 TF1-8 确立（勾组封面即组粒度），本条只是去掉人工编辑入口并把组还原成可视化条目；若选图回传协议需补充连拍组信息字段，在本条内一并扩展。
- **验收**：
  - [x] 新建用例选图后以卡片网格展示，连拍组显示 ×N，点开可精选、可移除
  - [x] 粒度全程自动：勾组封面得组粒度，精选后变单张粒度，保存后评估口径与改造前一致
  - [x] 旧用例（含组粒度）详情查看与评估不受影响
  - [x] vue-tsc 与 vite build 通过；用户手工验证选图与精选流程

### PS9 主题发现选图迁移到覆盖层

- **状态**：Done
- **方案**：[docs/design/2026-08-26-3-photo-selection-controls-hub.md](design/2026-08-26-3-photo-selection-controls-hub.md)
- **背景**：主题发现手动选题与管线步骤编辑用的是内嵌 SuggestPhotoSelector（全量拉取 + 前端搜索 + 网格多选），与控件1 并存成重复实现。用户决策：迁移到覆盖层形态，统一交互。
- **方案**：手动选题向导的选照片步骤与步骤编辑器的照片模式改为打开控件1 覆盖层（新发起方标识），已选预填充；「随机选取」保留在向导内作快捷按钮，不进通用覆盖层。迁移完成后停用并删除 SuggestPhotoSelector。
- **分析**：⚠️ 存在 id 口径差异：现有 SuggestPhotoSelector 提交的照片标识与控件1 回传口径不同（后者区分去后缀文件名与后端主键两种 id）。实施前先核对主题发现后端对 photo_ids 的实际消费口径，再决定映射方式，避免迁移后选题生成失效。向导内嵌布局改覆盖层属于交互形态变化，需用户手工验证顺手程度。
- **验收**：
  - [x] 手动选题 Step1 走覆盖层选图，已选预填充，随机选取仍可用
  - [x] 提交的照片 id 口径正确，选题可正常生成
  - [x] 步骤编辑器照片模式同步迁移，重跑管线不受影响
  - [x] SuggestPhotoSelector 删除后全项目无残留引用；vue-tsc 通过

### 导入工作流（Windows 客户端）

> W1-W11 已在 [v1.0.9](archive/v1.0.9.md) 归档。

### W12 ImportWorkflow.vue 拆分

- **状态**：待规划
- **背景**：`web/src/views/ImportWorkflow.vue` 已达 1010 行，三步流程全部单文件承载，存在持续的可维护性债务。
- **方案**：-
- **分析**：-
- **验收**：
  - [ ] 按三步拆为子组件，单文件行数降至合理范围
  - [ ] 拆分后三步流程行为不变

### GR1 三 Collection 向量库改造（组图检索）

- **状态**：WIP
- **方案**：[docs/design/2026-08-22-grouped-photo-retrieval.md](design/2026-08-22-grouped-photo-retrieval.md)
- **背景**：连拍分组已上线，但 Embedding 和向量检索仍以单张照片为单位，检索结果可能被同组照片占据。
- **分析**：新增精细组/模糊组集合，仅入库连拍组封面图；保留现有全量集合；检索、清理和前端展示按粒度切换。
- **验收**：
  - [x] 全量集合入库行为不变
  - [x] 精细组/模糊组仅包含对应连拍组封面图
  - [x] 聊天界面可切换检索粒度并展示组级结果
  - [x] 删除照片后三个集合均清理
  - [x] 连拍组重建后可更新组集合

### TF1-1 黄金用例粒度模型

- **状态**：Done
- **方案**：[docs/design/2026-08-25-1-eval-test-script-framework.md](design/2026-08-25-1-eval-test-script-framework.md#tf1-1-黄金用例粒度模型)
- **背景**：黄金照片引用没有记录检索粒度，无法表达同一问题在 photo、fine、coarse 三个 Collection 中的不同期望结果。
- **方案**：扩展黄金照片引用的概念模型与 JSON 读写，粒度缺省为 photo；导入、导出和已有保存入口统一保留该信息，并拒绝未知粒度。
- **分析**：这是后续评估按 Collection 分流的前置数据契约；采用默认值可兼容已有黄金 JSON，不做一次性迁移。
- **验收**：
  - [x] 新用例可保存并读取 photo/fine/coarse
  - [x] 旧 JSON 读取后默认按 photo 处理
  - [x] 导入导出不丢失粒度信息

### TF1-2 按粒度评估与单条运行

- **状态**：Done
- **方案**：[docs/design/2026-08-25-1-eval-test-script-framework.md](design/2026-08-25-1-eval-test-script-framework.md#tf1-2-按粒度评估与单条运行能力)
- **背景**：当前评估固定查询全量照片集合，且只能全量运行，无法验证组图检索或快速定位单条回归。
- **方案**：按黄金照片粒度分组目标集合，分别从对应向量集合检索并合并统一结果；保留全量评估，同时增加单条评估能力。
- **分析**：命中、未命中、多余命中必须使用同一套语义；单条与全量只改变输入范围，不复制指标逻辑。
- **验收**：
  - [x] photo/fine/coarse 使用对应集合评估
  - [x] 单条评估与全量评估结果结构一致
  - [x] 结果明确区分命中、未命中、多余命中
  - [x] 空标注和不存在用例有明确错误或边界结果

### TF1-3 三层 CLI 测试框架

- **状态**：Done
- **方案**：[docs/design/2026-08-25-1-eval-test-script-framework.md](design/2026-08-25-1-eval-test-script-framework.md#tf1-3-三层-cli-测试框架)
- **背景**：当前缺少不依赖浏览器和 LLM 的低 token 回归入口，GR1 的“未找到”无法快速下钻定位。
- **方案**：使用 Python 建立 L0 数据态、L1 函数级、L2 HTTP 级断言；支持按层运行和默认全套运行，统一输出短结论与失败层级。
- **分析**：三层分别回答“数据在不在、检索函数是否正确、接口是否完整”，共用黄金用例和检索模块，避免另造测试数据。
- **验收**：
  - [x] CLI 可选择 L0/L1/L2 并输出几行结论
  - [x] 失败信息包含用例、断言和失败层级
  - [x] 不调用 LLM、不启动浏览器
  - [x] 配置缺失或服务未启动时给出可定位错误

### TF1-4 检索与连拍粒度种子用例

- **状态**：Done
- **方案**：[docs/design/2026-08-25-1-eval-test-script-framework.md](design/2026-08-25-1-eval-test-script-framework.md#tf1-4-两类种子回归用例)
- **背景**：测试框架如果没有真实闭环用例，无法承载“佛像和人的合照”及三 Collection 回归。
- **方案**：沉淀一条检索闭环和一条连拍粒度闭环；用例固定问题、目标照片及排序/粒度断言，失败时可回溯到 L0/L1/L2。
- **分析**：种子用例是评估基建的最小可见产物；具体目标照片需以当前图库数据为准，不在规划阶段伪造 ID。
- **验收**：
  - [x] 至少有一条检索闭环用例
  - [x] 至少有一条 photo/fine/coarse 连拍粒度闭环用例
  - [x] 用例可直接由 CLI 运行并定位失败层级

### TF1-5 黄金用例评估与追加 API

- **状态**：Done
- **方案**：[docs/design/2026-08-25-1-eval-test-script-framework.md](design/2026-08-25-1-eval-test-script-framework.md#tf1-5-黄金用例-api-能力)
- **背景**：前端目前只有全量评估，不能单独运行一条用例，也不能把确认后的多余命中写回用例。
- **方案**：增加单条评估和追加期望照片能力，校验用例、照片和粒度输入；追加成功后立即持久化并返回更新后的用例或结果。
- **分析**：接口先固定数据语义，前端只负责展示和确认；保留现有全量评估接口，避免破坏已有入口。
- **实现**：
  - 评估结果贯通 `golden_id`：`_load_golden_queries_from_items` 保留用例 ID，`run_evaluation` 写入每条 detail，`EvalDetailItem` 透出给前端
  - 新增 `POST /api/golden-queries/{golden_id}/photos`，逻辑抽到模块级 `_append_photos_to_case` 便于单测
  - 照片以去后缀文件名为准，UUID 从 Go 后端实时解析，与评估匹配口径一致
  - 新建接口显式传入 `uuid` 时以其为准，ChatView 原有行为不变
- **验收**：
  - [x] 单条评估接口返回命中/未命中/多余命中
  - [x] 追加照片可指定粒度并持久化
  - [x] 追加后再次评估立即生效
  - [x] 不存在用例（404）、无效粒度（422）和不在图库的照片（400）有清晰错误

### TF1-7 黄金用例选图复用图片管理（覆盖层）

- **状态**：Done
- **方案**：[docs/design/2026-08-26-1-photo-pick-overlay.md](design/2026-08-26-1-photo-pick-overlay.md)
- **背景**：新建黄金用例选期望照片用的是内嵌简化列表（GoldenPhotoPicker），缺三档连拍折叠、分段、全选、区间选择等图片管理已有能力；继续堆功能等于造第三套重复图片列表。
- **方案**：需要选图时打开全屏覆盖层复用 PhotoListBrowser + usePhotos 完整交互，顶栏改写（隐藏 VLM/Embed/图文工坊等按钮，只留搜索/展示/筛选与「完成选择/取消」）；左侧菜单不切换；选中结果经 sessionStorage 会话回传，弹窗草稿留在父组件不丢；已选预填充、粒度保留。
- **实现**：
  - 新增 `web/src/utils/photoPickSession.ts`（sessionStorage 会话协议：create/read/complete/clear，draft 由发起方自定义）
  - 新增 `web/src/components/PhotoPickOverlay.vue`（Teleport 全屏覆盖层，复用 PhotoListBrowser + usePhotos，顶栏含展示级别/分段/排序/搜索筛选/全选/清空/区间选择/完成选择/取消）
  - `GoldenQueryManagement.vue` 新建弹窗「选择照片」按钮替换原内嵌选图器；打开覆盖层时弹窗隐藏、草稿入会话，完成恢复弹窗并按 photo_id 保留旧粒度；onMounted 检测残留会话恢复（F5 刷新场景）
  - 删除 `web/src/components/GoldenPhotoPicker.vue`
- **验收**：
  - [x] vue-tsc 与 vite build 通过
  - [ ] 用户手工验证：覆盖层交互完整性、草稿恢复、粒度保留、图片管理自身行为不变（见设计文档第 5 节）

### TF1-8 黄金用例选图连拍组粒度保持

- **状态**：Done
- **背景**：选图覆盖层复用图片管理后，折叠视图下勾选连拍组封面卡片，回传结果只带封面且默认单张粒度，黄金用例把「连拍组」存成单张 photo，与选择时不一致，评估也退化为查全量集合而非 fine/coarse 组集合。
- **方案**：选图覆盖层回传照片时按展示级别推导粒度（折叠视图勾选的连拍组封面给 fine/coarse，普通照片 photo）；预选照片保留发起方已设粒度；新建弹窗合并时以回传粒度优先。
- **分析**：根因在选图回传丢失粒度 + 弹窗对新照片默认 photo 两处；连拍组的黄金用例语义本就是「封面 + 粒度」单条 ref，无需展开组内全部成员。
- **实现**：
  - `photoPickSession.ts`：PickedPhoto 增加可选 granularity（photo/fine/coarse）
  - `PhotoPickOverlay.vue`：handleConfirm 按展示级别推导粒度，initFromPreselected 记录预选照片原粒度
  - `GoldenQueryManagement.vue`：onPickConfirm 以回传粒度优先，preselected 透传粒度
- **验收**：
  - [x] vue-tsc 通过
  - [ ] 用户手工验证：折叠视图勾选连拍组封面，返回后粒度自动为 fine/coarse

### TF1-9 黄金用例统一按单张照片处理

- **状态**：Done
- **背景**：黄金用例选图和已选区可以展示连拍组，但用例底层不应保存或评估连拍粒度；评估结果追加照片时的粒度下拉也会造成不必要的选择。
- **方案**：保留选图与已选区的连拍组展示和连拍精选。新建保存时，将未精选的连拍组展开为全部子图，精选后的条目直接按单张保存；黄金用例读取、追加、评估和导出统一按单张照片处理。新建已选区增加连拍展开提示，评估结果追加操作移除粒度选择。
- **验收**：
  - [x] 新建黄金用例时，未精选连拍组的全部子图写入底层数据
  - [x] 连拍精选后仅保存用户选中的单张照片
  - [x] 黄金用例读取和评估统一使用单张照片集合
  - [x] 未命中照片追加固定按单张处理，不再显示粒度下拉

### TF1-6 黄金用例管理界面

- **状态**：Done
- **方案**：[docs/design/2026-08-25-1-eval-test-script-framework.md](design/2026-08-25-1-eval-test-script-framework.md#tf1-6-黄金用例管理界面)
- **背景**：黄金用例依赖导入 JSON 或对话中保存，维护和校准成本高；列表行也无法单独评估。
- **方案**：在现有管理页增加新建表单、粒度选择、行级评估和明细中的追加操作；沿用现有全量评估与照片预览交互。
- **分析**：复用现有页面可保持用户入口和数据展示一致；追加操作必须由用户明确确认，避免把所有多余命中自动写入黄金集。
- **实现**：
  - 新增 `web/src/components/GoldenPhotoPicker.vue`（按文件名搜索、分页、多选），避免继续把选片逻辑堆进管理页
  - 管理页新增「新建」弹窗：查询文本 + 分类 + 备注 + 已选照片逐张选粒度
  - 列表行新增单条评估按钮，直接展开该条明细；详情弹窗按粒度分组展示关联照片
  - 明细中多余命中可勾选并选择粒度加入用例，追加后自动复评并按明细重算汇总指标
  - 导出补上 `granularity`，导入导出往返不再丢粒度
- **验收**：
  - [x] 可手动新建查询并添加带粒度的期望照片
  - [x] 每行可单独评估并展示三类结果
  - [x] 多余命中可确认后加入当前用例
  - [x] 保存后列表和评估明细即时刷新
  - [x] 旧用例查看、导入导出和全量评估继续可用

## 决策历史

- **2026-08-26**：选图控件统一立项（PS7-PS9）。确立两个标准控件：控件1 选图覆盖层（已落地）+ 控件2 已选照片列表（待抽取）。用户决策：黄金用例粒度完全自动推导不再人工编辑；主题发现选图迁移到覆盖层。复用约定与扩展决策流程见专题中枢。
- **2026-08-26**：TF1-7 立项。黄金用例选图不再用内嵌简化列表，改为覆盖层复用图片管理完整交互；sessionStorage 持久化会话、预填充已选。方案：[docs/design/2026-08-26-1-photo-pick-overlay.md](design/2026-08-26-1-photo-pick-overlay.md)
- **2026-08-26**：TF1-8 完成。选图覆盖层回传时推导连拍组粒度（折叠视图封面 → fine/coarse），修复「选择时是连拍组、返回后是单张 photo」的不一致。同时将 harness 方案确认规则改为：仅在存在多选或成本/后果较大时才询问用户。

- **2026-08-22**：v1.0.10 归档，完成连拍分组与照片列表浏览；6 项暂缓需求迁移至未来需求文档。
- **2026-08-23**：CL1 评估通过（8.4），CL2 修复 VLM HTTP 客户端 60 秒超时。
- **2026-08-24**：图文工坊 PS1–PS6 完成，覆盖选图、主题采纳、草稿、详情查看、导出和提示词重构。
- **2026-08-25**：v1.0.11 归档，CL1、CL2、PS1–PS6 已完成；W12、GR1 保留进入下一轮。TF1 评估测试脚本框架完成规划，拆分为 6 个子任务。
- **2026-08-25**：TF1-5、TF1-6 完成，黄金用例可在页面上新建、单条评估，并把确认的多余命中写回用例。TF1 六个子任务全部完成，下一步用回归脚本重查 GR1 的三 Collection 问题。
