// API 地址和上传配置常量

export const API_BASE = '/api/v1'

// Python Agent 地址（Embedding API）
export const AGENT_BASE = '/api'

// VLM 队列轮询间隔（ms）
export const VLM_POLL_INTERVAL = 1500

// Embed 队列轮询间隔（ms）
export const EMBED_POLL_INTERVAL = 2000

// 照片列表默认分页
export const DEFAULT_PAGE_SIZE = 24
