# Readme

## 最初思路

我们来构思一个全新的项目，现在的ai是文本能力较高，
图片/音频和视频依次能力降低，但是本来难度也高很多。
然后我之前在动漫公司也做过类似的事情，则：

首先对常见的角色图片进行视觉描述，这样就能建立起常见角色的文字描述。
以这些角色的描述为上下文，继续对图片用AI进行视觉描述，然后就可以通过文本搜索找到特定图片。
这些图片包括了原画、分镜图、制作输出图片、成片按镜头切割的截图等等。

这个应该算是一个ai应用吧，而有了这些内容后，就可以继续搭建一个ai agent应用，
本质上这些内容就是公司资产的数据库，包括文本/图片/音频/视频的都可以做到这个数据库里，
而这个数据库可以进一步做rag等技术，然后在此之上搭建ai agent，则比如可以辅助创作新剧情的剧本，乃至出图出视频。

刚好，我最近考虑后面的职业往ai应用开发，ai agent开发等ai方向发展，所以这个项目刚好能作为我的作品来展示。
你需要帮我展开这个思路和方案，既要写产品文档，也要写方案文档，还要写任务拆分。
并且在此前，先在当前这个readme文档，把需要确认的细节，可行性分析，设计是否合理，需求是否存在等问题讨论清楚，
讨论清楚这些前期问题，再来输出后续文档。

角色识别：我将人工标识角色，比如我会提供一批有规则命名的角色图片（一般是这个角色的制作输出的整/侧/俯视图）
数据规模：我有自己拍摄的照片作为输入，图片数量不超1万，但并不一定都要识别，只是我拥有这样的数据而已。如果顺利，我还想使用一个短片作为我的一个展示案例，比如使用爱死机短片中的一个，按镜头截图后进行识别得到文本描述。
应用场景：尽可能往Agent应用靠，首先仅仅是检索，本质上没必要用ai，有了文本描述，很多搜索技术可以搭建索引系统。然后生图生视频，核心在于模型，并不是我个人能做出来的成果。所以产品的形式应该是agent，基于这些数据，加上llm结合成一个agent应用，做思考，但限于文本输出，不用输出图和视频。
技术偏好：我没有具体的偏好，现在是初学者阶段。你可以通过本仓库的`DemoProject.md`了解到我的技术能力，`DemoProject.md`还包含了另一个demo的设计，但不用考虑进来，只需要了解我的技术能力，考虑本ai agent应用即可。
展示形态：实际上你说的我都要，但是首先是可运行的demo，开发这个demo过程中，当然就需要产出这些方案文档。实际上，这些文档也是我用于展示、辅助我自己开发、辅助我和ai共建开发。最后这个demo完成后，我也会总结一些技术文章上传到我的博客。但是现在，请考虑一个小团队或者一人团队，考虑这个demo需要的所有东西，包括产品设计，技术架构，任务拆分，代码实现，测试等等整个开发闭环。考虑这种ai辅助的新开发模式，我作为“总指挥”扮演开发过程的各个角色，把每个开发过程节点串起来，而实际上每个文档和代码乃至部署和测试等等，都将由ai完成。

## 项目定位

Media Agent 是一个**个人摄影资产 AI 助手**。通过对摄影作品进行 AI 视觉描述、建立时间线与标签知识库、构建向量检索系统，让用户可以用自然语言与自己的照片库对话，实现智能检索、摄影档案问答和创作辅助。

**求职方向**：AI 应用开发 / AI Agent 开发。

**开发模式**：AI 辅助开发，用户作为"总指挥"，AI 负责具体编码和文档输出。

---

## 前期讨论结论

### 技术栈：Go + Python 双栈

```
用户 → Python CLI (Click + Rich)
          ↓ HTTP
      Go Backend (Gin / 标准库)
          ├── 业务服务：素材管理、会话管理、导入任务
          ├── SQLite 数据访问
          └── 文件服务
          ↓ HTTP JSON
      Python AI Service (FastAPI + LangChain + Chroma)
          ├── VLM 视觉描述
          ├── LLM Agent 编排
          ├── 向量检索
          └── Embedding
```

- **Go 后端**：API 路由、请求校验、业务逻辑、SQLite CRUD、文件管理。复用用户 Go 工程经验和代码库。
- **Python AI 服务**：纯 AI 能力（VLM / LLM / 向量检索 / Embedding），通过 HTTP 接口对外暴露。
- **Python CLI**：调用 Go API，提供交互式命令行界面。
- **通信**：Go ↔ Python 通过 REST JSON API。

> 被否定的方案：纯 Python 单栈（后重新评估为 go+py 更利于复用代码库）、复杂分布式架构（MinIO/MySQL/Kratos）。详见 [docs/note.md](docs/note.md)。

### Agent 能力边界

```
P0 (MVP 必须): 智能检索 + 摄影档案问答
P1 (展示价值高): 摄影主题分析与创作建议
P2 (加分项): 时间线关联分析 + 个人摄影风格知识库
```

- **砍掉素材分析助手**：视觉 AI 厂商直接提供，无造轮子意义
- **不做人物身份识别**：亲友相貌相似，提高准确率成本太高，非核心展示场景
- **交互方式**：聊天 API + CLI，Web 界面后续再考虑

### 数据集

- **全部使用真实摄影素材**（约 300 张精选照片）
- 按时间线命名文件夹，如 `photos/2024-02-云南/`
- 真人短片截图仅作可选辅助展示

> 数据集决策详见 [docs/note.md](docs/note.md)。前期考虑过动漫/影视素材（爱死机 → Blender Sintel），已全部推翻。

### 项目优先级

- **优先处理 media_agent**，忽略 team-docflow（DemoProject.md 中的内容）
- 部署目标：前期本地 Docker，性能不足时搬迁云服务器
- 开发周期：5 个工作日（AI 辅助）

---

## 待确认清单

- [x] **技术栈**：Go + Python 双栈（Go 后端 + Python AI 服务 + Python CLI）
- [x] **AI 模型**：云端 API，无本地 GPU
- [x] **Agent 能力边界**：P0/P1/P2 如上，砍掉素材分析助手和角色一致性检查
- [x] **展示素材**：全部使用真实摄影素材
- [x] **向量数据库**：Chroma（Python 侧）
- [x] **业务数据库**：SQLite（Go 侧管理）
- [x] **项目优先级**：忽略 team-docflow，优先处理 media_agent
- [x] **部署目标**：前期本地 Docker 开发测试

---

## 已输出文档

| 文档 | 路径 | 内容 |
|------|------|------|
| 产品需求文档 | [docs/PRD.md](docs/PRD.md) | 产品定位、功能需求、数据需求、交互设计 |
| 技术方案文档 | [docs/TECH_SPEC.md](docs/TECH_SPEC.md) | Go + Python 架构设计、技术选型、数据模型、API 设计 |
| 开发任务拆分 | [docs/TASKS.md](docs/TASKS.md) | 5 天里程碑、每日任务清单、验收标准 |
| 决策备忘 | [docs/note.md](docs/note.md) | 被否定/推翻/变更的方案记录 |

---

## 下一步建议

**立即可做的准备工作**（不依赖代码）：

1. **整理摄影照片**：
   - 从 300 张精选照片中，挑选 100-200 张用于 demo
   - 按时间线命名文件夹，如 `photos/2024-02-云南/`
   - 格式建议：`{年份}-{月份}-{主题或地点}`

2. **（可选）准备真人短片截图**：
   - 找一个无版权真人超短片
   - 截取 20-30 张关键镜头，放入 `assets/shortfilm/` 目录

3. **准备 API Key**：
   - OpenAI（GPT-4o-mini）：[platform.openai.com](https://platform.openai.com)
   - 或 Qwen（通义千问）：[dashscope.aliyun.com](https://dashscope.aliyun.com)
   - 你已有 Kimi key，可作为备选

4. **开发环境**：
   - Go 1.22+
   - Python 3.11+
   - 创建 Python 虚拟环境：`python -m venv venv`

**如果你确认以上准备就绪，可以开始 Day 1 的开发**。告诉我是否需要直接开始写代码。
