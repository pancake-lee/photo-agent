# Backlog 全任务优化后复评

> 评估对象：当前 backlog 中全部 18 个 Done 任务（W12、PS7–PS9、GR1、CH1、TF1-1–TF1-10、QA1、M1）。
> 评估报告 ID：`2026-08-26-backlog-all-tasks-reassessment`。

## 摘要

- **总体评分：8.4/10，✅ 通过**（阈值 6.0）。所有任务均高于通过线，且无 1–3 分维度。
- 本轮优化有效：ImportWorkflow 与黄金用例管理页分别降至 716 行和 916 行；关键选图流程已有可重复浏览器回归；三 Collection 检索真实种子回归继续全通过。
- 新发现 1 项 P2：已废弃的 `GoldenPhotoPicker.vue` 仍在仓库，且关联记录互相矛盾，影响代码与文档的一致性。

## 分维度评分

### 代码质量：8.2/10

- **正确性：8.6**。Go 全量测试、Agent 相关单测 12/12、L0/L1/L2 两条真实种子回归均通过；黄金用例保存、更新和追加均以单张照片口径收敛。
- **健壮性：8.1**。三项服务重新启动后健康接口均为 HTTP 200；选图取消与草稿恢复已由无写入的浏览器脚本覆盖。详情更新的空查询和空照片集合亦有单测。
- **可维护性：8.2**。W12 与 M1 的职责拆分完成，父视图分别为 716 行、916 行；创建、详情、评估弹窗与步骤组件已有清晰边界。
- **简洁性：8.0**。选图覆盖层和已选列表被多个入口复用，避免再次复制图库逻辑。失分点是未引用的 `GoldenPhotoPicker.vue` 仍保留。

### 功能效果：8.3/10

- **准确性：8.6**。GR1 三 Collection 的数据态、函数级检索和 HTTP 契约均通过真实图库种子用例；单张黄金用例评估不再受历史粒度字段影响。
- **完整性：8.4**。覆盖层的打开、取消和草稿恢复已有浏览器证据，补齐了上次评估的关键缺口；导入三步流程和管理页拆分均通过生产构建。
- **一致性：8.0**。选图和已选照片展示复用统一控件，主题发现接入同一覆盖层。失分点是旧选图组件与相互矛盾的文档记录尚未收敛。

### 用户价值：8.1/10

- **惊喜度：7.7**。组图检索减少同连拍组占据结果的情况，能提供更高信息密度的照片结果。
- **可用性：8.7**。用户无需编辑 JSON 即可新建、编辑、单条评估及维护黄金用例；导入和选图流程的职责也更易理解。
- **交互体验：8.3**。覆盖层保留完整图库浏览能力，且取消后草稿可恢复；聊天缩略图和组内精选增加了结果可读性。
- **AI 增量：7.8**。三层回归不调用 LLM，但稳定保护了 AI 检索闭环；组集合对检索结果有明确增益。

## 分任务结论

- **W12 8.6、M1 8.7**：大视图拆分已落地，构建和回归均通过，明显改善后续维护边界。
- **PS7 8.3、PS8 8.3、PS9 8.2、QA1 8.7**：标准控件复用与主题发现迁移完成，浏览器脚本覆盖了覆盖层取消与草稿恢复。旧组件残留使此组一致性扣分。
- **GR1 8.6、CH1 8.2**：三集合真实回归持续通过；聊天缩略图逻辑与远程地址适配正确，但本轮没有覆盖缩略图点击预览及下载的浏览器自动化。
- **TF1-1 8.4、TF1-2 8.4、TF1-3 8.6、TF1-4 8.6、TF1-5 8.5、TF1-6 8.5、TF1-7 8.4、TF1-8 8.2、TF1-9 8.5、TF1-10 8.4**：数据模型、回归框架、管理 API 与界面形成闭环；TF1-9 的单张口径已在实现和单测中生效。相关历史设计记录仍有旧粒度/旧组件描述，降低文档一致性。

## 执行证据

- 三服务健康接口均返回 HTTP 200。
- `GOTOOLCHAIN=local go test ./...`：PASS。
- `agent/.venv/bin/python -m unittest tests.test_evaluation tests.test_eval_regression chain.test_post_studio_smoke`：12/12 PASS。
- `agent/.venv/bin/python scripts/eval_regression.py -c ../.local/pancake.yaml --level all`：L0、L1、L2 各 2 条真实种子用例 PASS。
- `web/pnpm build && pnpm test`：生产构建通过，Vitest 2/2 PASS。
- `node tools/golden_query_ui_regression.mjs`：PASS，验证选图覆盖层打开、取消与草稿恢复，不写入业务数据。

## 已登记问题

- P2：`GoldenPhotoPicker.vue` 仍在仓库且无入口引用；backlog 和设计文档对它同时保留“已删除”与“新增”的矛盾记录，当前实现与文档记录不一致。
